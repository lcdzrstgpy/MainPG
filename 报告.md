# 每日选品数据采集 UI 与后端接口对接报告

> 项目：`D:\xiangmu\MainPG-dev`  
> 核查日期：2026-08-05  
> 核查范围：`web-frontend` 的“每日选品”前端，以及 `local-runtime/wh_local/data_collection` 数据采集后端  
> 说明：本报告基于当前源码、正在运行的 FastAPI OpenAPI 和无外部额度消耗的本地连通性检查编写。

## 1. 结论摘要

当前“每日选品”前端需要的 5 类核心接口均已在本地后端注册：

1. 条件采集；
2. 批次列表；
3. 批次详情；
4. 候选排除反馈；
5. 候选确认并生成 handoff。

因此，从路由和数据模型角度看，关键词采集、参考图采集、候选展示、历史批次、人工排除和确认入池的基础对接面已经具备。

但当前环境还不能完成真实端到端采集，存在以下关键阻塞：

- **P0：OneBound 凭据未配置。** 当前配置检查结果为 `enabled=true`、`base_url` 已配置，但 `api_key` 和 `api_secret` 均为空。向正在运行的服务提交一份合法的 1688 关键词采集请求，实测返回 HTTP 500。
- **P0：确认接口缺少服务端候选状态校验。** 后端可按 ID 把 `filtered`、`rejected` 或带风险标签的候选直接改成 `confirmed`。前端虽然限制只能选择 `candidate`，但不能替代服务端约束。
- **P0：确认入池尚未真正进入当前运行中的产品处理模块。** 数据采集确认接口只创建 `daily_selection_handoffs` 记录。产品处理目录存在 handoff 消费实现，但当前 `wh_local.app.main.create_app()` 未注册产品处理路由，正在运行的 OpenAPI 中也没有对应消费接口。
- **P1：淘宝只是前端占位。** 后端条件模型只接受 `collection_platform="1688"`；前端选择淘宝或“1688 + 淘宝”时不会发请求。
- **P1：安全图片代理已注册但当前不可用。** 当前宿主没有向数据采集服务注入 `image_cache`，所以 `/desktop/daily-selection/image` 即使已开放，也无法返回图片；前端目前直接使用来源图片 URL。

建议先完成 P0 项，再安排真实接口联调。

## 2. 核查依据

### 2.1 前端主要文件

- `web-frontend/src/modules/daily_selection/pages/DailySelectionPage.tsx`
- `web-frontend/src/modules/daily_selection/api/dailySelectionApi.ts`
- `web-frontend/src/modules/daily_selection/types.ts`
- `web-frontend/src/modules/daily_selection/styles/daily-selection.css`
- `web-frontend/src/app/layout/WorkspaceShell.tsx`
- `web-frontend/vite.config.ts`

### 2.2 后端主要文件

- `local-runtime/wh_local/data_collection/routes.py`
- `local-runtime/wh_local/data_collection/criteria.py`
- `local-runtime/wh_local/data_collection/contracts.py`
- `local-runtime/wh_local/data_collection/service.py`
- `local-runtime/wh_local/data_collection/collector.py`
- `local-runtime/wh_local/data_collection/filtering.py`
- `local-runtime/wh_local/data_collection/scoring.py`
- `local-runtime/wh_local/data_collection/repository.py`
- `local-runtime/wh_local/data_collection/handoff.py`
- `local-runtime/wh_local/data_collection/plugin_queue.py`
- `local-runtime/wh_local/app/main.py`
- `local-runtime/wh_local/config.py`

### 2.3 运行状态核查

本地服务：`http://127.0.0.1:8000`

| 检查项 | 结果 |
| --- | --- |
| `GET /health` | HTTP 200，服务正常 |
| 当前 SQLite | `local-runtime/outputs/wh-local/workbench.sqlite3` |
| 带 `Bearer dev-admin-token` 获取批次 | HTTP 200，当前为 0 条 |
| 不带 Token 获取批次 | HTTP 401，`missing bearer token` |
| OpenAPI 中核心采集路由 | 已注册 |
| 合法 1688 关键词采集请求 | HTTP 500，原因是当前 OneBound key/secret 未配置 |

测试采集请求将 `target_count`、`max_api_calls` 和 `detail_count` 都设为 1；Provider 在凭据校验阶段即失败，没有发起有效的 OneBound 采集调用，也没有消耗 API 额度。

## 3. 当前前端 UI 功能清单

### 3.1 采集方向页

| UI 功能 | 当前实现 | 数据归属 | 是否需要后端 |
| --- | --- | --- | --- |
| 24 个默认采集方向 | 家居、厨房、清洁、卫浴等预设卡片 | 前端常量 | 否 |
| 添加自定义预设 | 名称、站点、关键词、属性、价格、数量、颜色 | `localStorage` | 当前不需要 |
| 删除默认或自定义预设 | 右上角进入删除模式，每次删除二次确认 | `localStorage` | 当前不需要 |
| 悬浮查看完整预设 | 显示站点、关键词、关注属性、价格、起订量和目标候选数 | 前端展示 | 否 |
| 点击预设打开采集面板 | 在工作台标签栏创建独立采集面板 | React 状态 | 否 |
| 多开采集面板 | 最多同时打开 6 个，每个面板状态独立 | React 状态 | 否 |
| 超过 6 个提示 | 阻止第 7 个并提示先关闭面板 | React 状态 | 否 |

注意：预设新增和删除是当前浏览器本地数据，不按用户或 workspace 同步。清缓存、换设备或换浏览器后不会同步。

### 3.2 采集面板

| UI 区域 | 功能 |
| --- | --- |
| 采集方式 | 关键词采集、参考图 URL 采集 |
| 采集平台 | 1688、淘宝、1688 + 淘宝；后两项目前只展示占位提示 |
| 站点 | 美国 `US`、哥伦比亚 `CO`、厄瓜多尔 `EC` |
| 选品范围 | 精准匹配 `exact`、发散相似款 `divergent` |
| 条件 | 关键词、参考图 URL、最低价、最高价、最低起订量、候选数量 |
| 风险开关 | 自动排除高风险候选 |
| 固定调用参数 | `max_api_calls=50`、`detail_count=10` |
| 开始采集 | 仅平台为 1688 时提交后端请求 |
| 最近批次 | 展示批次 ID、创建时间、候选数和状态，并可打开详情 |
| 候选商品 | 展示图片、标题、来源链接、价格、MOQ、店铺、产地、选品分、原因和风险标签 |
| 人工排除 | 对单个候选提交固定原因“前端人工排除” |
| 批量确认 | 勾选状态为 `candidate` 的商品并确认入池 |

### 3.3 前端已经声明但未充分展示的后端字段

后端候选还包含以下数据，当前 UI 没有详情入口或只展示其中一小部分：

- 商品图集 `source_image_urls`；
- 详情图 `source_detail_image_urls`；
- SKU/规格记录 `source_variant_records`；
- 来源属性 `source_attributes`；
- 运费 `freight_cny`；
- 销量文本、重量、包装信息；
- 已采集字段和缺失字段；
- 评分组成 `score_components`；
- API 审计证据 `evidence`；
- 批次元数据中的 API 调用数、错误和扩展词信息。

这些字段可以留给后续“候选详情抽屉”使用，不影响第一阶段基础联调。

## 4. 前端实际调用的接口

前端 API 封装位于 `dailySelectionApi.ts`。

| 前端函数 | 方法与路径 | UI 触发点 | 当前后端状态 |
| --- | --- | --- | --- |
| `collectByCriteria` | `POST /desktop/daily-selection/preview` | 开始采集 | 已注册；凭据未配导致当前实测 500 |
| `listSelectionRuns` | `GET /desktop/daily-selection/runs` | 面板加载、刷新、采集完成 | 已注册且可访问 |
| `getSelectionRun` | `GET /desktop/daily-selection/runs/{run_id}` | 点击历史批次、确认或排除后刷新 | 已注册 |
| `rejectCandidate` | `POST /desktop/daily-selection/runs/{run_id}/feedback` | 排除并反馈 | 已注册 |
| `confirmCandidates` | `POST /desktop/daily-selection/runs/{run_id}/confirm` | 确认入池 | 已注册；只生成 handoff |

所有请求使用：

```http
Authorization: Bearer <token>
Content-Type: application/json
```

当前前端 Token 来源为 `VITE_WH_API_TOKEN`，未配置时硬编码回退到 `dev-admin-token`。这适合本地演示，但正式登录后应改为使用客户登录接口返回的 session token。

Vite 开发服务器仅代理 `/desktop` 到 `http://127.0.0.1:8000`。后端没有配置 CORS，因此正式部署需要保持前后端同源、由桌面宿主转发，或显式增加受控的 CORS 配置。

## 5. 条件采集请求契约

### 5.1 前端当前发送示例

```json
{
  "keywords": ["盒", "架", "袋", "挂件"],
  "collection_mode": "keyword",
  "collection_platform": "1688",
  "selection_scope": "divergent",
  "category": "家居收纳",
  "min_price": 3,
  "max_price": 35,
  "min_moq": 2,
  "target_count": 20,
  "max_api_calls": 50,
  "detail_count": 10,
  "exclude_risks": true,
  "site": "US"
}
```

图片模式额外发送：

```json
{
  "collection_mode": "image",
  "reference_image_url": "https://example.com/public-image.jpg",
  "keywords": ["收纳", "置物"]
}
```

图片模式中的关键词是描述标签，不会额外触发关键词搜索。

### 5.2 后端校验规则

| 字段 | 后端规则 | 前端现状 |
| --- | --- | --- |
| `keywords` | 关键词模式必须 1–5 个；自动去空和去重 | 前端最多截取 5 个 |
| `collection_mode` | 仅 `keyword` / `image` | 已匹配 |
| `collection_platform` | 仅 `1688` | 仅 1688 会提交 |
| `selection_scope` | `exact` / `divergent` | 已匹配 |
| `reference_image_url` | 图片模式必填，必须为 HTTP(S) URL | 前端只做 URL 输入校验，后端有更严格安全下载 |
| `min_price` / `max_price` | 非负，最低价不能高于最高价 | 自定义预设有校验；采集表单主要依赖后端校验 |
| `min_moq` | 正整数 | 输入控件最小值为 1 |
| `target_count` | 正整数，无服务端上限 | 前端也没有最大值 |
| `max_api_calls` | 1–60 | 前端固定 50 |
| `detail_count` | 正整数，无服务端上限 | 前端固定 10 |
| `exclude_risks` | 严格布尔值 | 已匹配 |
| `site` | `US` / `CO` / `EC` | 已匹配 |

### 5.3 一个需要统一的业务语义

前端字段名称为“每个关键词”，但后端最终返回逻辑是：

1. 每个关键词分别搜索；
2. `divergent` 时还可能加入本地扩展词搜索；
3. 所有结果去重、过滤并统一评分；
4. 最终只对整个批次取前 `target_count` 个合格候选，再附上过滤记录。

因此 `target_count=20` 不是“每个关键词最终返回 20 个”，而更接近“整个批次最多保留 20 个合格候选”。建议把 UI 文案改为“目标候选数”，或由后端改成真正的按关键词配额并明确返回分组。

## 6. 后端已开放但当前前端未使用的能力

| 方法与路径 | 能力 | 前端是否需要 |
| --- | --- | --- |
| `POST /desktop/daily-selection/preview-from-1688-link` | 从 1688 商品链接提取详情，再按主图或标题采集相似商品 | 当前产品要求已明确移除“商品链接”UI，因此暂不接 |
| `GET /desktop/daily-selection/image` | 安全读取批次中已记录的图片 URL | 建议后续用于避免防盗链/CORS；当前宿主未注入缓存，实际不可用 |
| `POST /desktop/data-collection/plugin-sessions` | 创建浏览器插件会话 | 每日选品 UI 当前不需要 |
| `POST /desktop/data-collection/temu-link/collect` | 创建 Temu 链接采集命令 | 每日选品 UI 当前不需要 |
| `GET /desktop/data-collection/plugin/poll` | 插件领取命令 | 插件端使用 |
| `POST /desktop/data-collection/plugin/results` | 插件回传执行状态和结果 | 插件端使用 |
| `GET /desktop/data-collection/plugin-commands/{command_id}` | 查询插件命令状态和结果 | 若以后做 Temu 采集进度 UI 可接入 |

模块还保留了 `/plugin/connect`、`/plugin/poll`、`/plugin/result` 等兼容旧浏览器插件的路由。它们不应混入每日选品第一阶段前端契约。

## 7. 返回数据与 UI 映射

### 7.1 批次摘要

```ts
{
  run_id: string;
  workspace_id: string;
  status: string;
  candidate_count: number;
  created_at: string;
  updated_at: string;
}
```

前端用于最近批次列表。后端当前无分页或数量限制，按 workspace 返回全部批次。

### 7.2 完整批次

完整批次在摘要基础上增加：

- `criteria`：本次采集条件快照；
- `metadata`：API 调用数、错误、扩展规则和图片派生词；
- `candidates`：候选快照。

### 7.3 候选状态

| 状态 | 含义 | 当前 UI 行为 |
| --- | --- | --- |
| `candidate` | 可审阅候选 | 可勾选、可排除 |
| `filtered` | 被硬规则或风险规则过滤 | 展示但不可勾选 |
| `confirmed` | 已确认并创建 handoff | 展示但不可再次选择 |
| `rejected` | 人工排除 | 展示但不可再次选择 |

### 7.4 确认入池返回

确认接口返回 `DailySelectionHandoff[]`。每个 handoff 包含：

- `handoff_id`；
- `run_id`；
- `candidate_id`；
- `workspace_id`；
- `payload_json`；
- `status`，新建时为 `pending`；
- `idempotency_key`；
- `created_at`。

同一 workspace、批次和候选的重复确认是幂等的，会返回同一 handoff。

## 8. 接口覆盖矩阵

| UI 需求 | 后端接口 | 覆盖情况 | 说明 |
| --- | --- | --- | --- |
| 关键词采集 | `POST /preview` | 部分可用 | 路由和逻辑完整，当前缺凭据 |
| 参考图采集 | `POST /preview` | 部分可用 | 路由和安全下载逻辑完整，当前缺凭据 |
| 1688 平台 | `collection_platform=1688` | 已支持 | 唯一支持的平台 |
| 淘宝平台 | 无 | 未支持 | 前端占位，不发请求 |
| 组合平台 | 无 | 未支持 | 前端占位，不发请求 |
| 精准/发散 | `selection_scope` | 已支持 | 发散使用本地扩展词规则 |
| 价格/MOQ 过滤 | `min_price/max_price/min_moq` | 已支持 | 缺失价格或 MOQ 也会被过滤 |
| 风险排除 | `exclude_risks` | 已支持 | 但确认接口仍需服务端二次校验 |
| 候选排序评分 | 本地 filtering/scoring | 已支持 | 确定性评分 |
| 最近批次 | `GET /runs` | 已支持 | 无分页 |
| 批次详情 | `GET /runs/{run_id}` | 已支持 | workspace 隔离 |
| 人工排除 | `POST /feedback` | 已支持 | 当前前端原因固定 |
| 确认入池 | `POST /confirm` | 部分可用 | 创建 handoff；尚未进入当前运行的产品处理模块 |
| 预设增删 | 无 | 前端本地实现 | 不跨设备同步 |
| 最多 6 个面板 | 无 | 前端本地实现 | 后端无须支持 |
| 安全图片代理 | `GET /image` | 路由存在、运行不可用 | 缺少 `image_cache` 注入 |
| 采集进度/取消 | 无 | 未支持 | 当前采集请求为同步调用 |
| 批次删除/归档 | 无 | 未支持 | 历史批次只读 |

## 9. 发现的问题与建议优先级

### P0：联调前必须解决

#### 9.1 配置 OneBound，并把配置错误转成可识别的 HTTP 响应

当前缺少：

- `DAILY_SELECTION_ONEBOUND_API_KEY`；
- `DAILY_SELECTION_ONEBOUND_API_SECRET`。

可使用环境变量或 Git 忽略的 `wh_local/onebound.local.json`。不要把真实密钥提交到仓库、报告或前端环境变量。

同时建议后端捕获 Provider 配置错误，返回例如：

```json
{
  "detail": {
    "code": "PROVIDER_NOT_CONFIGURED",
    "message": "1688 采集服务尚未配置"
  }
}
```

推荐 HTTP 503，而不是当前的无说明 HTTP 500。

#### 9.2 服务端必须限制可确认候选

`confirm_candidates()` 当前不检查候选原状态和 `risk_tags`，会直接改成 `confirmed`。建议仅允许：

- `status == "candidate"`；
- `risk_tags` 为空，或由明确的管理员复核参数授权；
- 候选属于当前 workspace 和 run。

不满足时返回 409 或 422，并给出候选 ID 和原因。

#### 9.3 接通 handoff 消费闭环

当前确认只完成：

```text
候选 confirmed → daily_selection_handoffs.pending
```

项目中存在产品处理 handoff 消费实现：

```text
POST /intake/daily-selection/handoffs
```

但该路由未注册到当前 FastAPI 应用，OpenAPI 中不可见。需要确定一种方案：

1. 注册产品处理 router，由前端确认后显式调用消费接口；或
2. 后端使用队列/后台消费者自动消费 `pending` handoff；或
3. 数据采集确认接口在同一宿主中调用产品处理 service，并保持幂等。

在闭环完成前，前端文案应保持“等待产品处理模块消费”，不能显示“已进入产品草稿池”。

### P1：接口语义和运行保障

#### 9.4 统一 `target_count` 文案与语义

建议第一阶段直接把前端“每个关键词”改为“目标候选数”，改动最小且符合当前后端行为。

#### 9.5 为数量字段增加上限

建议后端增加：

- `target_count` 合理上限，例如 100；
- `detail_count <= max_api_calls`，并增加合理上限；
- 前端输入框同步设置 `max`。

#### 9.6 决定图片加载策略

当前有两种不一致方案：

- 前端直接加载 `candidate.main_image_url`；
- 后端提供受控 `/desktop/daily-selection/image`。

如果使用安全代理，需要在宿主注入 `DailySelectionImageCache`，并让前端用 `run_id + url` 生成代理 URL，同时附带鉴权。否则应明确移除或暂不承诺该路由。

#### 9.7 正式登录 Token 对接

移除前端默认的 `dev-admin-token` 依赖，统一从登录状态读取 session token；401 时跳回登录或刷新会话。

#### 9.8 统一 API 错误结构

前端目前只优先读取字符串形式的 `detail`。建议统一：

```ts
type ApiError = {
  detail: string | {
    code: string;
    message: string;
    fields?: Record<string, string>;
  };
};
```

前端应兼容 FastAPI 校验错误数组，避免只显示“请求失败（422）”。

#### 9.9 明确调用预算是“单批次”还是“每日”

当前宿主默认注入的是 `TaskApiBudget`，只限制一次采集任务；并未使用 `SQLiteDailyApiBudget` 做跨批次的每日累计限制。前端文案写的是“每批最多调用 50 次”，与当前代码一致，但 `data_collection/README.md` 中关于每日预算的描述容易造成误解。

需要产品和后端共同确认：

- 只限制单批 50 次；或
- 同时增加 workspace/凭据维度的每日总额度。

#### 9.10 批次列表增加分页

`GET /runs` 当前返回 workspace 的全部历史批次，前端每个打开的采集面板都会请求一次。建议增加 `limit`、`cursor` 或 `offset`，第一屏默认 20 条。

### P2：体验与可维护性

#### 9.11 多面板历史状态可能不一致

最多 6 个面板各自请求并保存批次列表。一个面板完成采集后，只刷新自身列表，其它已打开面板仍是旧数据。可接受于第一阶段；后续可引入共享查询缓存或统一刷新事件。

#### 9.12 预设是否需要账号同步

若预设属于个人效率工具，`localStorage` 足够；若需要团队共享、权限、排序和跨设备同步，则需新增独立的 preset CRUD 接口。不要把预设写入采集批次表。

#### 9.13 增加数据采集模块回归测试

当前工作区中未发现 `data_collection` 对应测试文件，只看到客户认证相关测试。建议至少覆盖：

- 关键词/图片条件校验；
- Provider 缺配置和禁用状态；
- workspace 数据隔离；
- 过滤、风险和评分；
- feedback 状态变更；
- confirm 状态约束和幂等；
- 图片代理 SSRF 防护；
- 插件会话隔离和命令状态机；
- OpenAPI 路由注册冒烟测试。

## 10. 建议的第一阶段联调流程

### 后端准备

- [ ] 配置 OneBound key/secret，确认仅由后端读取。
- [ ] Provider 未配置时返回结构化 503。
- [ ] 修复确认接口的状态和风险校验。
- [ ] 确认 handoff 消费方案并注册产品处理接口或后台消费者。
- [ ] 提供一套 Fake Provider 联调开关，避免每次 UI 调试消耗真实额度。
- [ ] 明确 `target_count` 的最终语义。
- [ ] 确认图片直链还是安全代理。

### 前端准备

- [ ] 把“每个关键词”改为“目标候选数”，除非后端改成逐关键词配额。
- [ ] 接入真实登录 session token。
- [ ] 兼容结构化错误和 FastAPI 422 错误数组。
- [ ] 淘宝继续保持“不调用后端”的明确提示，或暂时隐藏选项。
- [ ] 确认入池成功后，根据最终消费方案显示“等待消费”或“已创建草稿”。
- [ ] 若采用安全图片代理，切换候选图片 URL 生成逻辑。

### 联调验收用例

1. 单关键词精确采集，验证条件、候选数和批次落库。
2. 5 个关键词发散采集，验证 API 调用数不超过限制且结果去重。
3. 公网参考图采集，验证 URL 安全策略和图搜结果。
4. 最低价、最高价和 MOQ 边界测试。
5. 开关风险过滤，验证风险候选状态和确认限制。
6. 人工排除后重新读取批次，确认状态为 `rejected` 且反馈落库。
7. 多选确认，验证 handoff 数量、幂等键和产品处理消费结果。
8. 两个 workspace 使用同一 run ID/候选 ID，验证互相不可见。
9. 同时打开 6 个采集面板，验证表单状态互不覆盖。
10. Token 失效、Provider 未配置、上游超时、额度不足和部分成功的错误展示。

## 11. 建议的对接完成标准

满足以下条件后，可以认为“每日选品”前后端第一阶段完成：

- 关键词和参考图都能在 Fake Provider 与真实 Provider 环境成功创建批次；
- 前后端字段、数量语义和错误结构一致；
- 候选只能按服务端规则确认；
- 确认后的 handoff 能被产品处理模块幂等消费并生成可见草稿；
- 用户登录 Token 和 workspace 隔离生效；
- 淘宝能力要么真实接入，要么保持清晰的未开放状态；
- 核心流程具备自动化回归测试，日常前端调试不消耗真实 OneBound 额度。

---

## 附录：当前最小前后端职责边界

```text
前端
  采集方向与本地预设
  最多 6 个独立采集面板
  条件输入与基础校验
  批次/候选展示
  人工选择、排除、确认操作

数据采集后端
  身份与 workspace 隔离
  条件强校验
  OneBound 调用与额度控制
  规范化、去重、过滤、评分
  批次与候选快照
  反馈与确认状态约束
  handoff 幂等创建

产品处理后端
  消费 pending handoff
  幂等创建产品草稿
  更新或记录消费结果
```
