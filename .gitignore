# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# 测试与缓存产物
.pytest_cache/
.DS_Store

# IDE
.vscode/
.idea/
.worktrees/

# Local runtime artifacts
local-runtime/outputs/
local-runtime/.runtime-packages/
outputs/

# PyInstaller 打包产物
local-runtime/dist/
local-runtime/build/

# Local secrets
local-runtime/wh_local/onebound.local.json
local-runtime/wh_local/modules/product_processing/cos.local.json
local-runtime/_rsa_private.pem
local-runtime/_rsa_public.pem
local-runtime/wh_local/secrets.py

# 妙手导出的运行时产物：精简模板缓存与参考表由程序首次导出时自动生成
local-runtime/wh_local/modules/product_processing/templates/generated/
local-runtime/wh_local/modules/product_processing/templates/妙手参考表-类目ID与包装清单.xlsx

# 嵌入式 Python 运行时（打包用，体积大，不进 git）
local-runtime/python-runtime/

# 运行时日志
*.log

# 测试临时目录
local-runtime/.pytest-*/

# 本地备份
_backup/

# Local tooling
local-runtime/python.exe
.trae/
product_processing.db

# 公告发布后台本地数据
announcement-admin/announcements.sqlite3
announcement-admin/config.json

# 运行时输出与锁文件
real-workbench/employee_workbench/product_processing/
*.lock
!package-lock.json
!yarn.lock
!pnpm-lock.yaml

# pytest 残留目录（ACL 损坏，git 无法遍历，避免 warning）
.pytest-update-admin-*/

# 项目工作数据
.workbuddy/

# 测试残留的临时 SQLite 文件
NONEXISTENT-NO-DB

# 本地运行时业务日志（login.log / ai_processing.log / pod_processing.log / runtime.log）
local-runtime/*.log
