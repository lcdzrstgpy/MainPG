{
    "name": "canvas-plugin-clipforge",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "description": "ClipForge video node: turn canvas material into a finished commerce short video via a local ClipForge instance",
    "scripts": {
        "build": "node build.mjs",
        "dev": "node build.mjs --watch",
        "typecheck": "tsc --noEmit"
    },
    "devDependencies": {
        "@infinite-canvas/plugin-sdk": "file:../sdk",
        "@types/react": "19.1.12",
        "typescript": "^5"
    }
}