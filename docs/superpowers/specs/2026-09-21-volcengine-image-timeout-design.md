# 火山引擎九宫格生图超时设计

## 目标

避免火山引擎同步生图在高负载时被通用 30 秒客户端超时误判为失败。

## 方案

仅在 `VolcEngineProvider.generateImage` 调用方舟 `/images/generations` 时传入 120 秒超时。视频任务创建、状态轮询和其他服务仍使用既有时限与“付费 POST 不自动重试”规则。

## 验收

- 火山生图请求携带 120 秒超时；
- 火山视频请求不因本改动改变超时或重试语义；
- 适配器回归测试与生产构建通过。
