# 整店采集链路审阅与 ID 数据溯源（2026-09-08）

> 审阅对象：`local-runtime/wh_local/data_collection/` 下整店采集（Whole-Shop Collection）的 1688 链路与淘宝链路。
> 目的：把"四个接口如何两两配合"讲清楚，并回答"采集需要的 ID 到底取自哪里"——用两条真实链接（1688 `917498330422`、淘宝 `874362279873`）实际跑了解析代码验证。

---

## 0. 一句话结论

- **四个接口**：`item_get`（身份解析 + 链接详情，一条接口两种用途）、`item_search_shop`（1688 店铺列表）、`item_search_shop_pro`（淘宝店铺全量列表）、`seller_info`（淘宝兜底补 seller_id）。
- **两两配对**：1688 = `item_get` ↔ `item_search_shop`；淘宝 = `item_get`(+`seller_info`) ↔ `item_search_shop_pro`。
- **ID 数据来源**：
  - **商品 ID（offer_id / item_id）→ 来自你粘贴的 URL**（1688 取 `/offer/<id>.html` 路径，淘宝取 `?id=<id>`）。
  - **店铺 SID / shop_id / seller_id / 店名 → 全部来自 OneBound `item_get` 的响应体**（1688：`item.seller_info.sid`；淘宝：`item.shop_id` / `item.seller_id` / `item.nick`；淘宝缺 seller_id 时由 `seller_info` 接口补）。
  - **URL 里没有任何店铺 ID 数据**——已用两条真实链接实际执行解析代码验证：两个 `extract_*_shop_*` 均报"did not include a shop SID / numeric shop id"。
- **当前最核心缺陷**：`provider.py:_outcome_for_status`（671 行）把 OneBound 的真实成败字段 `error_code`（`0000`=成功、`2000`=无结果、`4000/4001/4002/4003`、`5000`=错误）漏读了，读的是不存在的顶层 `code`/`msg`/`message`，导致**所有 HTTP-200 响应（含真实上游错误）都被判为"成功"**，店铺身份/详情解析因此拿到错误数据，产生一系列误导性报错（"未返回店铺信息（shop_id）"、"item detail did not include an offer ID"、"万邦接口返回失败"、"0 商品静默完成"）。

---

## 1. 整体架构：三段式状态机

整店采集由后台 `ShopCollectionWorker.process_batch`（`shop_worker.py`）按批次状态推进：

```
queued → resolving → listing → enriching → completed / partial / failed
                                             ↑ 有失败     ↑ 全成功
```

| 阶段 | 干什么 | 调用的接口 |
|---|---|---|
| `resolving` | 用粘贴的"种子商品"调 `item_get`，解析出**店铺身份**（SID / shop_id / seller_id / 店名） | `item_get`（必要时 `seller_info`） |
| `listing` | 用店铺身份调**店铺商品列表**，逐页抓商品 ID 落库 | `item_search_shop` / `item_search_shop_pro` |
| `enriching` | 对列表里的每个商品 ID 再调 `item_get`（**链接详情**），规范化写入草稿池 | `item_get`（每商品一次） |

**关键认知**：`item_get` 一条接口在 `resolving` 和 `enriching` 各打一次——这就是为什么"整店采集看起来只要两个接口"（身份/详情 + 店铺列表），而实际上四个接口在两次 `item_get` 之间循环。

---

## 2. 四个接口总览

| 接口（operation） | 端点 | 请求参数 | 平台 | 用途 / 阶段 |
|---|---|---|---|---|
| `item_get` | `{base_url}/item_get/` | `num_iid=<商品ID>` | 1688 / 淘宝共用 | ① `resolving`：从种子商品解析店铺身份；② `enriching`：逐个商品取链接详情 |
| `item_search_shop` | `{base_url}/item_search_shop/` | `seller_nick=<店铺SID>, page` | 1688 | `listing`：店铺商品列表 |
| `item_search_shop_pro` | `{base_url}/item_search_shop_pro/` | `shop_id, seller_id, page` | 淘宝 | `listing`：店铺全量商品列表（高级版；普通版 `item_search_shop` 会 4005 无权访问） |
| `seller_info` | `{base_url}/seller_info/` | `shop_id` | 淘宝 | `resolving` 兜底：`item_get` 没返回 `seller_id` 时补 | 

请求入口对应代码：

- `provider.py:423 get_item_detail` → `item_get`
- `provider.py:433 search_shop` → 1688 `item_search_shop`；淘宝 `item_search_shop_pro`
- `provider.py:468 get_seller_info` → `seller_info`
- `provider.py:759 _endpoint` → `{base_url}/{operation}/` 拼接

---

## 3. ID 数据来源溯源（本次重点）

### 3.1 输入解析：URL → 商品 ID（已验证）

用两条真实链接实际执行 `wh_local.data_collection.shop_parsing` / `shop_service`：

```
URL A（1688）: https://detail.1688.com/offer/917498330422.html?offerId=917498330422&...
URL B（淘宝）: https://item.taobao.com/item.htm?id=874362279873&...
```

实际运行结果：

| 检查项 | URL A（1688） | URL B（淘宝） |
|---|---|---|
| 平台判定 `detect_shop_platform` | `1688` | `taobao` |
| **商品 ID** `extract_1688_offer_id` / `extract_taobao_item_id` | **`917498330422`**（取自路径 `/offer/917498330422.html`，兜底 `offerId` 参数） | **`874362279873`**（取自 `?id=` 参数） |
| **店铺 SID** `extract_1688_shop_sid` | ❌ `1688 shop URL did not include a shop SID` | — |
| **店铺 shop_id** `extract_taobao_shop_id` | — | ❌ `taobao shop URL did not include a numeric shop id` |
| `create_batch` 解析结果 `(shop_sid, seed_offer_id, shop_url)` | `('pending:917498330422', '917498330422', <URL>)` | `('pending:874362279873', '874362279873', <URL>)` |

结论：

- **URL 只能给出商品 ID**（1688：路径 `/offer/<id>.html` 或参数 `offerId`；淘宝：参数 `id` 或 `item_id`/`itemId`/`num_iid`/`offer_id`，见 `shop_parsing.py:25` 与 `shop_parsing.py:222`）。
- **URL 不能给出店铺 ID**。它们不是店铺首页链接，`extract_*_shop_*`（仅在 `shop{id}.taobao.com` 子域名 / `shop_id` 参数 / winport 等形态时可用）必然失败。
- 因此 `create_batch` 把 `shop_sid` 记为 **`pending:<商品ID>`** 占位——**"店铺 ID"是待解析状态，必须等 `resolving` 阶段用 `item_get` 响应来定**（`shop_service.py:160 _parse_1688_source` 第 180 行、`shop_service.py:199 _parse_taobao_source`，占位前缀 `pending:` 就是"还没解析出店铺身份"的意思）。

### 3.2 店铺身份：100% 取自 `item_get` 响应体（已验证）

`resolving` 阶段对种子商品调 `item_get`，从响应体里读店铺身份：

**1688**（`shop_worker.py:166 _resolve_shop` → `shop_worker.py:600 _seller_info`）：

```
item_get(num_iid=917498330422) 响应 →
  item = payload.item（或 payload.data / payload 兜底）
  seller = item.seller_info
  sid        = seller.sid                    ← 店铺 SID 取自这里
  shop_name  = seller.shop_name or seller.nick   ← 店名取自这里
```

- 拿不到 `seller_info.sid` → 批次失败，错误 `item detail did not include a shop SID`。

**淘宝**（`shop_worker.py:197 _resolve_taobao_shop` → `shop_worker.py:609 _taobao_shop_identity`）：

```
item_get(num_iid=874362279873) 响应 →
  item = payload.item / payload.data / payload.items.item（兼容套娃）
  seller = item.seller_info
  shop_id    = item.shop_id / item.shopId / seller.shop_id / seller.shopId
               └ 都没有时：seller.zhuy / seller.url 里的 shop{id}.taobao.com 子域名
  seller_id  = item.seller_id / item.sellerId / seller.seller_id / seller.sellerId
  shop_name  = seller.shop_name / seller.nick / item.nick          ← 店名
```

真实样本佐证（本库 `shop_collection_items` 成功样本 `1013099286546`，心相印启枫专卖店）：

```
item.shop_id        = 362234128          ← shop_id 在 item 级，不在 seller_info 里
item.seller_id      = 2218900641075      ← seller_id 也在 item 级
item.nick           = 心相印启枫专卖店
item.seller_info.zhuy = https://shop362234128.taobao.com/   ← 兜底提取 shop_id 用
```

- 拿不到 `shop_id` → 批次失败，报"该商品详情未返回店铺信息（shop_id）——返回字段：顶层[…]，item[…]……"（`shop_worker.py:662 _taobao_identity_failure_message`，会嵌入响应实际字段名，可诊断）。

### 3.3 `seller_info` 兜底（仅淘宝）

`item_get` 有 `shop_id`、但缺 `seller_id` 时（`shop_worker.py:223-236`）：

```
seller_info(shop_id=362234128) 响应 →
  payload.items / payload.data → seller_id、shop_id、nick
```

`shop_worker.py:683 _seller_info_identity` 依次读 `seller_id`、`shop_id`、`nick`。还缺 `seller_id` → 报"未能解析该店铺的卖家信息（seller_id）……"。

### 3.4 ID 数据流总图

```
粘贴的 URL
   │  extract_1688_offer_id / extract_taobao_item_id（URL 解析）
   ▼
商品ID: 917498330422 / 874362279873        shop_sid = pending:<商品ID>（占位）
   │  item_get(num_iid=<商品ID>)           ← 接口① 第一次调用（resolving）
   ▼
店铺身份: seller_info.sid（1688）          shop_id + seller_id + nick（淘宝）
   │  缺 seller_id → seller_info(shop_id)  ← 接口④ 兜底（仅淘宝）
   ▼
item_search_shop(seller_nick=SID)          item_search_shop_pro(shop_id, seller_id)
   │  ← 接口② / 接口③ 按页抓取（listing）
   ▼
offer_id 列表（逐页落库 shop_collection_items）
   │  item_get(num_iid=<每个offer_id>)     ← 接口① 第二次调用（enriching）
   ▼
链接详情 → normalize_detail_response → 写入产品处理草稿池
```

### 3.5 输入形态对照：商品链接 vs 店铺链接 vs 裸店铺主页（实测）

直接用两条真实店铺链接与商品链接执行解析代码（`shop_parsing` / `shop_service`）：

- 淘宝店铺链接：`https://shop204191181.taobao.com/category.htm?…`
- 1688 店铺链接：`https://shop739ub02816486.1688.com/?…&offerId=917498330422&…`

| 输入形态 | 能提取商品 ID | 能提取店铺 ID | create_batch 解析结果 | 后续链路 |
|---|---|---|---|---|
| 淘宝店铺链接 `shop204191181.taobao.com/category.htm` | ❌ | ✅ `204191181`（`shop{id}.taobao.com` 子域名 → `extract_taobao_shop_id`） | `('204191181', '', <url>)` **店铺 ID 已定** | `seller_info(204191181)` 补 seller_id → `item_search_shop_pro(204191181, seller_id)` |
| 1688 店铺链接（**带** `offerId=917498330422`） | ✅ `917498330422`（URL **query 参数** `offerId` → `extract_1688_offer_id`） | ✅ `shop739ub02816486`（子域名，但**不会被采用**） | `('pending:917498330422', '917498330422', <url>)` → **被当商品链接** | `item_get(917498330422)` → `seller_info.sid` → `item_search_shop` |
| 1688 裸店铺主页（**去掉** offerId） | ❌ | ✅ `shop739ub02816486` | `('shop739ub02816486', '', <url>)` | **直接** `item_search_shop(seller_nick=shop739ub02816486)` ← ⚠️ 危险 |
| 淘宝商品链接 `?id=874362279873` | ✅ `874362279873` | ❌ | `('pending:874362279873', …, <url>)` | `item_get` → `item.shop_id`/`item.seller_id` |
| 1688 商品链接 `/offer/917498330422.html` | ✅ `917498330422` | ❌ | `('pending:917498330422', …, <url>)` | `item_get` → `item.seller_info.sid` |

**结论（回答"只给商品链接够不够"）：**

1. **商品链接够不够？——够，两条链路都够**（前提：商品真实在售且 OneBound 返回 `error_code=0000`）。OneBound 真正需要的店铺身份**全部来自 `item_get` 响应体**：淘宝 `item.shop_id` + `item.seller_id`；1688 `item.seller_info.sid`（`b2b-…` 形式，工作样本 `b2b-3375644855fbfbb` 证明它正是 `item_search_shop` 要的 `seller_nick`）。URL 里只有商品 ID，没有店铺身份。
2. **淘宝店铺链接**：能直接抽出 `shop_id=204191181`，够用，但 `item_search_shop_pro` 需要 `shop_id + seller_id` 两个参数，`seller_id` 必须靠 `seller_info` 接口补 → 多一次调用、多一个失败点（`seller_info` 失败 = 整批失败）。
3. **1688 店铺链接**：因为携带 `offerId` 参数，系统实际按**商品链接**解析（提取 `917498330422` 当种子）→ 够用，等价于商品链接。
4. **1688 裸店铺主页**：`extract_1688_shop_sid` 提出的 `shop739ub02816486` 是 **1688 Web 店铺主页 ID，不等于 OneBound 的 `seller_nick`（`b2b-…`）**。拿它调 `item_search_shop` 很可能 0 结果或失败——"艺妮饰品 0 商品静默完成"这类异常，根源就是身份标识没定对/列表返回空。
5. **平台页面 URL 里的"店铺 ID"与 OneBound 接口要的身份标识不是一码事**：1688 `shop739ub02816486` ≠ `seller_nick(b2b-…)`；淘宝 `204191181` 是同一个 shop_id，但 `item_search_shop_pro` 还缺 `seller_id`。**最稳输入 = 店内真实在售商品链接。**

---

## 4. 1688 链路（配对一：`item_get` ↔ `item_search_shop`）

| 步 | 代码 | 接口 | 说明 |
|---|---|---|---|
| 1 | `shop_service.create_batch` → `_parse_source_input` | — | URL → `(pending:<id>, <id>, url)` |
| 2 | `shop_worker._resolve_shop`（166） | `item_get` | `num_iid=<id>`，读 `item.seller_info.sid` → `resolve_shop_identity(shop_sid)` |
| 3 | `shop_worker._list_shop`（264） | `item_search_shop` | `seller_nick=<sid>, page`，逐页到 `max_pages`/`total_pages` |
| 4 | `shop_parsing.normalize_shop_page` | — | 只抽 `offer_ids` + 分页元数据（**不保留标题/URL**） |
| 5 | `shop_repository.record_shop_page`（272） | — | 落库 `offer_id`（source_url/title 为空） |
| 6 | `shop_worker._enrich_one`（381） | `item_get` | 每 offer 一次；`normalize_detail_response` 规范化 → intake 写草稿池 |

⚠️ 1688 分支的坑：`_list_shop` 里"第 1 页 0 条就报错"的守卫**只有淘宝分支有**（`shop_worker.py:293` 明确 `if batch.platform == "taobao" and not values and page == 1`）。1688 列表返回 0 时是**静默 completed、0 发现**——"艺妮饰品"批次即为此现场。

---

## 5. 淘宝链路（配对二：`item_get`(+`seller_info`) ↔ `item_search_shop_pro`）

| 步 | 代码 | 接口 | 说明 |
|---|---|---|---|
| 1 | `shop_service.create_batch` → `_parse_taobao_source` | — | URL → `(pending:<id>, <id>, url)` |
| 2 | `shop_worker._resolve_taobao_shop`（197） | `item_get` | `num_iid=<id>`，`_taobao_shop_identity` 读 `shop_id`/`seller_id`/`nick` |
| 2b | 缺 `seller_id` 时（223-236） | `seller_info` | `shop_id=<shop_id>` 补 `seller_id` |
| 3 | `shop_worker._list_shop`（264） | `item_search_shop_pro` | `shop_id, seller_id, page`（淘宝只开通 pro 全量） |
| 4 | 第 1 页 0 条（仅淘宝守卫，293-299） | — | 报可诊断错误："万邦可能未收录这家店/受限类目/需开通接口" |
| 5 | `shop_worker._enrich_one`（381） | `item_get` | 每 item 一次 → 规范化 → 写草稿池 |

---

## 6. 两两配对的依赖规则

1. **接口 B（列表）依赖接口 A（`item_get`/`seller_info`）的输出**：
   - 1688：`item_search_shop` 需要 `seller_nick = seller_info.sid`；
   - 淘宝：`item_search_shop_pro` 需要 `shop_id + seller_id`。
   - 接口 A 失败 → 接口 B 无参数可用 → 整批失败（淘宝）或列表空（1688）。
2. **接口 B 的输出又被接口 A 消费**：列表只产 `offer_id` 集合，真正进草稿池的详情来自第二轮 `item_get`。
3. 所以链路是 **A 定身份 → B 扫全店 → A 逐个取详情**；`item_get` 是唯一的"重载"接口，在两端各出现一次。
4. **成败判定贯穿所有接口**：每个接口的响应都要过 `provider._outcome_for_status`（671）。OneBound 用 `error_code`（`0000` 成功 / `2000` 无结果 / 非 0000 错误），但该函数读的是顶层 `code`/`msg`/`message`（OneBound 根本不存在这两个字段，成功样本顶层只有 `error_code`/`error`/`reason`）。**所有 HTTP-200 响应都被判成"成功"**，真实的 `error_code≠0000` 漏网 → 下游解析器拿到畸形数据，报出误导性错误。

---

## 7. 已知问题与坏例映射

| 坏例 | 平台 | 断点 | 表现 | 根因 |
|---|---|---|---|---|
| 淘宝 `pending:1064709712149` | 淘宝 | resolving 第 2 步 | 报"该商品详情未返回店铺信息（shop_id）"，实际 `item_get` 返回 `error_code=5000 reason=data error item={format_check}` | `_outcome_for_status` 把上游错误误判成功 → 找不到 shop_id → 误导性报错 |
| 1688 艺妮饰品（`b2b-2207904953815045a9`） | 1688 | listing 第 3 步 | 种子 `item_get` 成功定到 SID，但 `item_search_shop` 返回 0 → **静默"采集完成，0 商品"** | 1688 无空页守卫；且列表接口若返回 `error_code≠0000` 也会被误判成功 → 解析 0 项 |
| 整店采集 12/51（心相印启枫，`362234128`） | 淘宝 | enriching 第 6 步 | `item detail did not include an offer ID` | 部分商品 `item_get` 返回 `error_code≠0000`/空壳被误判成功，normalizer 解析不到 num_iid |
| 插件采集 49/60（dc7ee7b5） | 1688 | enriching | `upstream_failed · 万邦接口返回失败` | 上游非 200/未知错误码折叠成单一 `upstream_failed`，真实原因丢失 |

**共因**：`_outcome_for_status`（`provider.py:671`）读错字段。该函数只在 HTTP 状态 / `code` / `msg` / `message` 上判断，而 OneBound 的状态载体是顶层 `error_code`（`0000`/`2000`/`4000-4003`/`5000`）+ `error`/`reason`。成功响应（本库样本）顶层字段为 `item, error, …, reason, error_code, api_info, …`，**没有 `code`/`msg`/`message`**，所以：

- HTTP 200 + 任何 `error_code` → `code=""` → 判 `success`（真实错误漏网）；
- 非 200（如 404）→ 折叠成 `upstream_failed`（真实原因丢失）；
- 设计文档 `docs/superpowers/specs/2026-09-07-daily-selection-taobao-onebound-integration-design.md:26` 声称"现有 `_outcome_for_status` 已覆盖"，**与实际实现不符**。

---

## 8. 修复（已实施，2026-09-08）

4 项建议已全部落地：

1. **`provider.py:_outcome_for_status`**：优先读顶层 `error_code`（`0000`→`success`、`2000`→`no_results`、其它数值码按 HTTP 状态/原因文本细化分类，默认 `upstream_failed`）；旧 `code`/`msg` 分支兼容保留。真实上游错误不再被误判成功。
2. **上游错误透传**：`ProviderCallResult.error.context` 新增 `upstream_code`/`upstream_reason`（`http_status`/`request_id` 原有）；`shop_worker._result_upstream_code` 读取并用于诊断文案（`_resolve_taobao_shop`、`_result_error_message`）。
3. **1688 列表空页守卫**：`shop_worker._list_shop` 的"第 1 页 0 条报错"从淘宝专属改为全平台，1688 提示"店铺 SID 是否 OneBound 可用的 seller_nick（b2b-… 形式）"。
4. **可读文案**：`plugin_onebound_capture._provider_failure_message` 优先展示真实 `error_code`（新增 `_ONE_BOUND_ERROR_REASONS` 映射 `4000/4001/4002/4003/4005/0800`）；无上下文时保持原笼统文案。

**测试**：
- `tests/test_shop_collection_provider.py`：新增 `test_outcome_classifies_onebound_error_code_over_legacy_fields`（6 组：`0000`/`2000`/`5000`/`4005`/旧 `code=200`/`code=2000`，含 context 断言）。
- `tests/test_shop_collection_integration.py`：新增 `test_1688_empty_first_page_fails_batch_instead_of_silent_success`。
- `tests/test_plugin_taobao_capture.py`：新增 `test_item_failure_surfaces_onebound_error_code_instead_of_generic_reason`。
- `tests/test_shop_collection_worker.py`：`test_listing_never_fetches_more_than_one_hundred_pages` 适配空页守卫（第 1 页非空、余页空 `has_next=True`，保留 100 页封顶断言）。

**回归结果**：相关 146 个测试全绿；全量 `1079 passed / 67 failed`，失败集与 HEAD 基线完全一致（均为本 checkout 环境预置问题，如 `wh_local.secrets` 缺失、`test_topup_promotion.py` 引用不存在的 billing 导出、Doubao/AI 网关相关，与本次改动无关）。

---

## 9. 附录：关键代码位置索引

| 文件 | 位置 | 内容 |
|---|---|---|
| `shop_service.py` | 41 / 135 / 160 / 199 / 146 | create_batch / _parse_source_input / _parse_1688_source / _parse_taobao_source / _resolve_platform |
| `shop_parsing.py` | 25 / 222 / 49 / 240 / 275 | extract_1688_offer_id / extract_taobao_item_id / extract_1688_shop_sid / extract_taobao_shop_id / detect_shop_platform |
| `shop_worker.py` | 166 / 197 / 264 / 313 / 381 / 600 / 609 / 683 / 505 | _resolve_shop / _resolve_taobao_shop / _list_shop / _enrich / _enrich_one / _seller_info / _taobao_shop_identity / _seller_info_identity / _default_detail_normalizer |
| `provider.py` | 423 / 433 / 468 / 671 / 759 | get_item_detail / search_shop / get_seller_info / **_outcome_for_status（根因）** / _endpoint |
| `shop_repository.py` | 168 / 272 / 314 / 350 / 373 | resolve_shop_identity / record_shop_page / claim_pending_items / complete_item / fail_item |
| `shop_contracts.py` | 23 / 56 | ShopBatch / ShopBatchItem 契约 |
| 设计文档 | `docs/superpowers/specs/2026-09-07-…-taobao-onebound-integration-design.md:26` | 错误码约定（"已覆盖"与实际不符） |
